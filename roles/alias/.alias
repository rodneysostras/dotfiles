   alias ..="cd .."
   alias cat="cat"
   alias ls="exa --icons --group-directories-first"
   alias ll="exa --icons --group-directories-first -lah"
   # alias code="code ."
   alias dev="devcontainer open"
   alias di="echo dotenv > .envrc && touch .env && direnv allow"
   # alias jup="jupyter notebook --no-browser"
   alias port="lsof -i -P -n"
   alias pkill="kill -9 $(lsof -t -i:8080)"
   alias unpm="find . -name 'node_modules' -type d -prune -exec rm -rf '{}' ';'"
   alias inpm="find . -name 'package.json' -type f -not -path '*/node_modules/*' -execdir npm ci --silent --no-progress \;"
   alias awslc="aws --endpoint-url=${LOCALSTACK_URL:-http://localhost:4566} --profile=localstack"
   alias akill="top -b -n 1 | awk 'NR>7 && $9 > 40 {print $1}' | xargs -I {} kill {}"
   alias projects="cd /mnt/wsl/PHYSICALDRIVE1/projects"
